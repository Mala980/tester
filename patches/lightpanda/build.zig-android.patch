diff --git a/build.zig b/build.zig
index 9e9f54e..43dc9a8 100644
--- a/build.zig
+++ b/build.zig
@@ -229,6 +229,13 @@ fn addExe(b: *Build, config: ExeConfig, name: []const u8, check_name: []const u8
         }),
     });
 
+    // Android requires position-independent executables; Zig 0.16 defaults to
+    // static non-PIE for `aarch64-linux-android`, which bionic rejects.
+    if (config.target.result.abi.isAndroid()) {
+        exe.pie = true;
+        exe.linkage = .dynamic;
+    }
+
     const exe_check = b.addLibrary(.{
         .name = check_name,
         .root_module = exe.root_module,
@@ -325,6 +332,7 @@ fn linkV8(
 
 fn linkRust(b: *Build, mod: *Build.Module) void {
     const is_debug = mod.optimize.? == .Debug;
+    const is_android = mod.resolved_target.?.result.abi.isAndroid();
 
     // One cargo workspace, one staticlib (src/rust/Cargo.toml explains why).
     const exec_cargo = b.addSystemCommand(&.{
@@ -334,6 +342,11 @@ fn linkRust(b: *Build, mod: *Build.Module) void {
         "--manifest-path", "src/rust/ffi/Cargo.toml",
     });
 
+    // Cross-compiling for Android: cargo must target the android triple
+    if (is_android) {
+        exec_cargo.addArgs(&.{ "--target", "aarch64-linux-android" });
+    }
+
     addDirInputs(b, exec_cargo, "src/rust", "target") catch |err| {
         std.debug.panic("walk src/rust: {t}", .{err});
     };
@@ -353,7 +366,10 @@ fn linkRust(b: *Build, mod: *Build.Module) void {
     const rust_step = b.step("rust", "Build the Rust staticlib (requires cargo)");
     rust_step.dependOn(&exec_cargo.step);
 
-    const obj = out_dir.path(b, if (is_debug) "debug" else "release").path(b, "liblightpanda_ffi.a");
+    const obj = if (is_android)
+        out_dir.path(b, "aarch64-linux-android").path(b, if (is_debug) "debug" else "release").path(b, "liblightpanda_ffi.a")
+    else
+        out_dir.path(b, if (is_debug) "debug" else "release").path(b, "liblightpanda_ffi.a");
     mod.addObjectFile(obj);
 }
 
